# TeamLease EdTech Referral Portal - UI Dockerfile
# For AWS ECS Fargate Deployment
# Security-hardened production build

# Build stage
FROM node:18-alpine AS build

WORKDIR /app

# Copy package files
COPY frontend/package*.json ./

# Install dependencies (including devDependencies for build)
RUN npm ci --ignore-scripts

# Copy source code
COPY frontend/ .

# Build argument for API URL (passed at build time or use default)
ARG VITE_API_URL=http://localhost:8000
ENV VITE_API_URL=${VITE_API_URL}

# Build for production
RUN npm run build

# Production stage - using nginx:stable-alpine for security
FROM nginx:stable-alpine

# Security: Remove default nginx configs
RUN rm -rf /etc/nginx/conf.d/default.conf \
    && rm -rf /usr/share/nginx/html/*

# Copy built files
COPY --from=build /app/dist /usr/share/nginx/html

# Copy custom nginx configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Security: Create nginx cache directories with proper permissions
RUN mkdir -p /var/cache/nginx/client_temp \
    && mkdir -p /var/cache/nginx/proxy_temp \
    && mkdir -p /var/cache/nginx/fastcgi_temp \
    && mkdir -p /var/cache/nginx/uwsgi_temp \
    && mkdir -p /var/cache/nginx/scgi_temp

# Security: Set proper permissions
RUN chown -R nginx:nginx /var/cache/nginx \
    && chown -R nginx:nginx /var/log/nginx \
    && chown -R nginx:nginx /usr/share/nginx/html \
    && chmod -R 755 /usr/share/nginx/html

# Security: Make nginx.conf readable
RUN chown nginx:nginx /etc/nginx/conf.d/default.conf

# Security: Remove unnecessary packages and files
RUN apk del --no-cache curl wget \
    && rm -rf /var/cache/apk/*

# Expose port
EXPOSE 80

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=10s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost/health || exit 1

# Run nginx (as root to bind port 80, but worker processes run as nginx user)
CMD ["nginx", "-g", "daemon off;"]
